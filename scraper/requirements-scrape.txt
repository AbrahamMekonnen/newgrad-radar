# Core dependencies for the job scraper (radar.py) only.
# The heavy auto-apply deps (playwright, browser-use, langchain-*) live in
# requirements.txt and are NOT needed to scrape and write jobs.
requests>=2.31.0
supabase>=2.0.0
python-dotenv>=1.0.0
google-generativeai>=0.3.0
dnspython>=2.4.0
beautifulsoup4>=4.12.0
telethon>=1.34.0
pypdf>=4.0
python-calamine>=0.2  # fast .xlsx parsing for DOL H1B LCA disclosure files
# Content decoders for the stealth session's Accept-Encoding (it advertises
# br/zstd). Without these, requests can't decode br/zstd responses and .content
# is raw compressed bytes — which then poison the response cache. Required.
brotli>=1.1
zstandard>=0.22

pywebpush>=1.14  # Web Push (browser/PWA notifications) via VAPID
