# Core dependencies for the job scraper (radar.py) only.
# The heavy auto-apply deps (playwright, browser-use, langchain-*) live in
# requirements.txt and are NOT needed to scrape and write jobs.
requests>=2.31.0
supabase>=2.0.0
python-dotenv>=1.0.0
google-generativeai>=0.3.0
dnspython>=2.4.0
beautifulsoup4>=4.12.0
telethon>=1.34.0
pypdf>=4.0
