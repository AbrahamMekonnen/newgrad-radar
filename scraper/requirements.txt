requests>=2.31.0
supabase>=2.0.0
python-dotenv>=1.0.0
google-generativeai>=0.3.0
dnspython>=2.4.0
beautifulsoup4>=4.12.0
# Content decoders for the stealth session's Accept-Encoding (br/zstd).
# Without these, requests can't decode br/zstd responses (e.g. USAJobs) and
# the body is raw compressed bytes -> parse fails -> 0 jobs.
brotli>=1.1
zstandard>=0.22

# Auto-apply agent
browser-use>=0.1.0
langchain-google-genai>=1.0.0
langchain-groq>=0.1.0
langchain-ollama>=0.1.0
playwright>=1.40.0
